import 'package:flutter_two_part1/main.dart';

class Customer {
  void Hello() {
    Student s1; // create object s1
    new Student(); // instantiate = create instance
    s1 = Student(); // instantiate + point s1 to instance
    s1.show();
  }
}
